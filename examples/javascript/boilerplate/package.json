{
  "name": "boilerplate",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "author": "FanConnect",
  "scripts": {
    "build": "parcel build src/index.html --dist-dir ./dist",
    "clean": "rm -rf ./dist",
    "watch": "parcel watch src/index.html src/config.json --dist-dir ./dist --no-hmr"
  },
  "devDependencies": {
    "@fanconnecttv/preview": "^0.2.1",
    "@parcel/core": "2.8.2",
    "@parcel/transformer-sass": "2.8.2",
    "parcel": "2.8.2",
    "parcel-reporter-clean-dist": "^1.0.4"
  },
  "targets": {
    "default": {
      "publicUrl": "./"
    }
  }
}
